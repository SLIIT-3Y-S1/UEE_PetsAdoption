import 'package:flutter/material.dart';

class LostPetHomeTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lost pet Home'),
      ),
      body: Center(
        child: Text('Content for lost pet tab'),
      ),
    );
  }
}